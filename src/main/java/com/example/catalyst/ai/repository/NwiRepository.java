package com.example.catalyst.ai.repository;

import com.example.catalyst.ai.model.NWI;
import org.springframework.data.jpa.repository.JpaRepository;

public interface NwiRepository extends JpaRepository<NWI, Long> {
}
